# Dependencies & build
node_modules/
web/dist/
server/target/
target/
*.local

# Runtime uploads
server/data/

# Local pdfium binary (Linux dev; deploy scripts fetch their own)
server/libpdfium.so

# Python sidecar venv / test artifacts
python/.venv/
python/.testout/
**/.venv/

# Deploy staging / Windows service helper (binaries, built dist)
deploy/stage/
deploy/**/*.exe
deploy/.env

# Docs (local only; deploy/README.md is tracked)
/README.md
wiki/

# Agent tooling (not part of the product)
.claude/
.cursor/

# Logs / OS / editors
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Tauri build artifacts
desktop/gen/
